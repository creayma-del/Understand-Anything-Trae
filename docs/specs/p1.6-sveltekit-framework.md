# P1.6: SvelteKit 框架配置详细设计规格

> 版本: 1.0
> 日期: 2026-06-05
> 状态: 已评审（修复 HIGH 问题后）
> 前置: P0.2（Svelte 语言配置 + SveltePlugin）已完成

## 1. 目标与范围

为项目新增 SvelteKit 框架的自动检测和架构层级映射，包括：

- FrameworkConfig 配置注册（框架 ID、检测关键词、层级映射）
- 框架检测逻辑（基于 `package.json` 依赖关键词匹配）
- Skill 文档（SvelteKit 项目结构、边模式、架构层、特有模式）
- 注册变更（`builtinFrameworkConfigs` 数组新增条目）

**本规格不涉及**：

- 新增语言配置（Svelte 语言配置已在 P0.2 中完成）
- 新增解析器/插件（SveltePlugin 已在 P0.2 中完成）
- 新增依赖（无第三方包引入）
- 修改现有接口签名

## 2. SvelteKit 项目结构分析

SvelteKit 是基于 Svelte 的全栈应用框架，采用文件系统路由约定，核心特征如下：

### 2.1 路由约定文件

```
src/routes/
├── +page.svelte          # 路由页面组件（必选）
├── +page.ts              # 页面 load 函数（客户端/通用）
├── +page.server.ts       # 页面服务端 load 函数
├── +layout.svelte        # 布局组件（包裹子路由）
├── +layout.ts            # 布局 load 函数
├── +layout.server.ts     # 布局服务端 load 函数
├── +error.svelte         # 错误边界组件
├── +loading.svelte       # 加载状态组件（SvelteKit 2+）
├── +server.ts            # API 路由处理器（GET/POST/PUT/DELETE 等）
├── about/
│   └── +page.svelte      # /about 路由页面
├── api/
│   └── users/
│       └── +server.ts    # /api/users API 端点
└── (auth)/               # 路由分组（不影响 URL）
    ├── login/
    │   └── +page.svelte
    └── register/
        └── +page.svelte
```

### 2.2 核心约定文件语义

| 文件模式 | 语义 | 运行环境 |
|---|---|---|
| `+page.svelte` | 路由页面 UI 组件 | 客户端 + 服务端（SSR） |
| `+page.ts` | 页面 load 函数（通用） | 客户端 + 服务端 |
| `+page.server.ts` | 页面 load 函数（仅服务端） | 仅服务端 |
| `+layout.svelte` | 布局组件，包裹子路由 | 客户端 + 服务端（SSR） |
| `+layout.ts` | 布局 load 函数（通用） | 客户端 + 服务端 |
| `+layout.server.ts` | 布局 load 函数（仅服务端） | 仅服务端 |
| `+error.svelte` | 错误边界组件 | 客户端 + 服务端 |
| `+loading.svelte` | 加载状态组件 | 客户端 |
| `+server.ts` | API 路由处理器 | 仅服务端 |

### 2.3 模块别名系统

| 别名 | 解析路径 | 用途 |
|---|---|---|
| `$lib` | `src/lib` | 项目共享代码（组件、工具函数、服务） |
| `$app/environment` | 框架内置 | 运行环境信息（`browser`、`dev`、`prerendering`） |
| `$app/stores` | 框架内置 | 页面 store（`page`、`navigating`、`updated`） |
| `$app/paths` | 框架内置 | 路径配置（`base`、`assets`） |
| `$app/navigation` | 框架内置 | 导航函数（`goto`、`invalidate`、`pushState`、`replaceState`） |
| `$app/server` | 框架内置 | 服务端工具（`redirect`、`error`、`json`、`fail`） |

### 2.4 项目根目录结构

```
my-sveltekit-app/
├── src/
│   ├── routes/            # 文件系统路由（核心）
│   ├── lib/               # $lib 别名指向的共享代码
│   │   ├── components/    # 共享 UI 组件
│   │   ├── server/        # 仅服务端代码
│   │   └── index.ts       # $lib 入口
│   ├── params/            # 自定义参数匹配器
│   ├── hooks.server.ts    # 服务端钩子（handle、handleFetch）
│   ├── hooks.client.ts    # 客户端钩子（handleError）
│   └── app.html           # HTML 模板
├── static/                # 静态资源
├── svelte.config.js       # SvelteKit 配置（含 adapter）
├── vite.config.ts         # Vite 配置
└── package.json           # 依赖声明
```

## 3. Layer 1: FrameworkConfig 实现

### 3.1 文件: `packages/core/src/languages/frameworks/sveltekit.ts`

```typescript
import type { FrameworkConfig } from "../types.js";

export const sveltekitConfig = {
  id: "sveltekit",
  displayName: "SvelteKit",
  languages: ["typescript", "javascript", "svelte"],
  detectionKeywords: ["@sveltejs/kit", "@sveltejs/adapter-"],
  manifestFiles: ["package.json", "svelte.config.js", "svelte.config.ts"],
  promptSnippetPath: "./frameworks/sveltekit.md",
  entryPoints: [
    "src/routes/+layout.svelte",
    "src/routes/+page.svelte",
    "src/hooks.server.ts",
    "src/hooks.client.ts",
  ],
  layerHints: {
    "src/routes": "ui",
    "src/lib/components": "ui",
    "src/lib": "service",
    "src/hooks": "middleware",
    "src/params": "utility",
    "static": "resource",
  },
} satisfies FrameworkConfig;
```

### 3.2 配置字段说明

| 字段 | 值 | 说明 |
|---|---|---|
| `id` | `"sveltekit"` | 框架唯一标识，用于检测匹配和 Skill 路由 |
| `displayName` | `"SvelteKit"` | 用户可见的框架名称 |
| `languages` | `["typescript", "javascript", "svelte"]` | SvelteKit 项目使用的语言 ID 列表，`"svelte"` 对应 P0.2 中注册的 LanguageConfig |
| `detectionKeywords` | `["@sveltejs/kit", "@sveltejs/adapter-"]` | 在 `package.json` 的 dependencies/devDependencies 中搜索的关键词 |
| `manifestFiles` | `["package.json", "svelte.config.js", "svelte.config.ts"]` | 框架检测时扫描的清单文件 |
| `promptSnippetPath` | `"./frameworks/sveltekit.md"` | Skill 文档路径，相对于 `skills/understand/` 目录 |
| `entryPoints` | 4 个入口文件 | SvelteKit 项目的典型入口点 |
| `layerHints` | 6 条映射 | 目录名 → 架构层级的映射规则 |

### 3.3 detectionKeywords 设计理由

- **`@sveltejs/kit`**: SvelteKit 核心依赖，所有 SvelteKit 项目必须安装
- **`@sveltejs/adapter-`**: SvelteKit adapter 前缀（如 `@sveltejs/adapter-auto`、`@sveltejs/adapter-node`、`@sveltejs/adapter-static`），使用前缀匹配而非完整包名，覆盖所有 adapter 变体

**不使用的关键词及理由**:

| 排除的关键词 | 理由 |
|---|---|
| `"svelte"` | 过于宽泛，纯 Svelte 项目（无 SvelteKit）也会匹配 |
| `"vite"` | 过于宽泛，所有 Vite 项目都会匹配 |
| `"svelte.config"` | 非纯 Svelte 项目也可能有此文件，且不是 package.json 中的依赖关键词 |

### 3.4 manifestFiles 设计理由

- **`package.json`**: 包含 `@sveltejs/kit` 依赖声明，是框架检测的主要依据
- **`svelte.config.js` / `svelte.config.ts`**: SvelteKit 项目特有的配置文件，包含 adapter 配置和预渲染选项，作为辅助检测依据

### 3.5 layerHints 映射详解

| 目录模式 | 层级 | 说明 |
|---|---|---|
| `src/routes` | `ui` | 所有路由文件（+page/+layout/+error/+loading）属于 UI 层 |
| `src/lib/components` | `ui` | 共享 UI 组件属于 UI 层，优先匹配（比 `src/lib` 更具体） |
| `src/lib` | `service` | 共享逻辑代码（工具函数、服务端代码、store）属于 Service 层 |
| `src/hooks` | `middleware` | 服务端/客户端钩子属于 Middleware 层 |
| `src/params` | `utility` | 自定义参数匹配器属于 Utility 层 |
| `static` | `resource` | 静态资源文件属于 Resource 层 |

**匹配优先级**: `layerHints` 中更具体的路径优先匹配。例如 `src/lib/components/foo.svelte` 优先匹配 `src/lib/components → ui`，而非 `src/lib → service`。这与现有框架配置（如 Next.js 的 `app` vs `app/api`）的处理方式一致。

## 4. Layer 2: 框架检测逻辑

### 4.1 检测流程

框架检测由 `FrameworkRegistry` 自动完成，流程如下：

```
1. 扫描项目根目录下的 manifestFiles
   → package.json: 解析 dependencies + devDependencies
   → svelte.config.js / svelte.config.ts: 检查文件是否存在

2. 对每个 FrameworkConfig 的 detectionKeywords 进行匹配
   → "@sveltejs/kit": 检查 package.json 中是否存在以此开头的依赖键
   → "@sveltejs/adapter-": 检查 package.json 中是否存在以此开头的依赖键

3. 匹配成功 → 返回 sveltekitConfig
   匹配失败 → 继续检查其他 FrameworkConfig
```

### 4.2 检测关键词匹配规则

`detectionKeywords` 的匹配规则与现有框架配置一致：

- 关键词在 `package.json` 的 `dependencies` 或 `devDependencies` 的**键名**中搜索
- 匹配方式为**包含匹配**（`includes`），即 `"@sveltejs/kit"` 匹配键名 `"@sveltejs/kit"`，`"@sveltejs/adapter-"` 匹配 `"@sveltejs/adapter-auto"`、`"@sveltejs/adapter-node"` 等
- 只需匹配到**一个**关键词即判定为该框架

### 4.3 与 Svelte 语言配置的关系

| 维度 | Svelte 语言配置（P0.2） | SvelteKit 框架配置（P1.6） |
|---|---|---|
| 配置类型 | `LanguageConfig` | `FrameworkConfig` |
| ID | `"svelte"` | `"sveltekit"` |
| 职责 | 文件级语言识别（`.svelte` → Svelte） | 项目级框架检测（SvelteKit 项目识别） |
| 依赖关系 | 独立 | `languages` 引用 `"svelte"` |
| 检测时机 | 文件扫描时（按扩展名） | 项目扫描时（按 package.json） |
| 层级映射 | 无（语言配置不提供层级映射） | 提供 `layerHints` 目录→层级映射 |

### 4.4 与其他框架的互斥关系

SvelteKit 框架与现有框架**天然互斥**：

| 框架 | 互斥原因 |
|---|---|
| React | 不同 UI 库 |
| Next.js | 依赖 React |
| Express | 服务端框架，不含 Svelte |
| Vue | 不同 UI 库 |

一个项目不会同时被检测为 SvelteKit 和其他框架。

## 5. Layer 3: Skill 文档

### 5.1 文件: `skills/understand/frameworks/sveltekit.md`

```markdown
# SvelteKit Framework Addendum

> Injected into file-analyzer and architecture-analyzer prompts when SvelteKit is detected.
> Do NOT use as a standalone prompt — always appended to the base prompt template.

## SvelteKit Project Structure

When analyzing a SvelteKit project, apply these additional conventions on top of the base analysis rules.

### Canonical File Roles

| File / Pattern | Role | Tags |
|---|---|---|
| `src/routes/+page.svelte` | Route page component — renders UI for a URL path | `entry-point`, `ui`, `routing` |
| `src/routes/+page.ts` | Page load function — fetches data before page renders (universal) | `service`, `routing` |
| `src/routes/+page.server.ts` | Server-only page load function — accesses databases, secrets | `service`, `routing`, `server` |
| `src/routes/+layout.svelte` | Layout component — wraps child routes with shared UI (nav, sidebar) | `ui`, `config`, `routing` |
| `src/routes/+layout.ts` | Layout load function — provides shared data to nested routes (universal) | `service`, `routing` |
| `src/routes/+layout.server.ts` | Server-only layout load function | `service`, `routing`, `server` |
| `src/routes/+error.svelte` | Error boundary component — renders when load functions throw | `ui`, `routing` |
| `src/routes/+loading.svelte` | Loading state component — shown during navigation transitions | `ui`, `routing` |
| `src/routes/**/+server.ts` | API route handler — exports GET/POST/PUT/PATCH/DELETE functions | `api-handler`, `server` |
| `src/hooks.server.ts` | Server hooks — `handle` intercepts requests, `handleFetch` proxies external requests | `middleware`, `server` |
| `src/hooks.client.ts` | Client hooks — `handleError` catches unhandled errors | `middleware` |
| `src/lib/components/**/*.svelte` | Shared UI components — reusable across routes | `ui` |
| `src/lib/server/**/*.ts` | Server-only utility code — database, auth, secrets | `service`, `server` |
| `src/lib/*.ts`, `src/lib/**/*.ts` | Shared utility functions, stores, type definitions | `service`, `utility` |
| `src/params/*.ts` | Custom parameter matchers — validate dynamic route segments | `utility`, `routing` |
| `src/app.html` | HTML shell template — contains `%sveltekit.head%` and `%sveltekit.body%` | `config` |
| `svelte.config.js`, `svelte.config.ts` | SvelteKit configuration — adapter selection, prerender, CSRF settings | `config` |
| `static/**` | Static assets — served as-is, no processing | `resource` |

### Edge Patterns to Look For

**Layout nesting** — When `src/routes/+layout.svelte` wraps `src/routes/about/+layout.svelte`, which wraps `src/routes/about/+page.svelte`, create `contains` edges from each layout to its child routes/layouts. Layouts compose via the file-system hierarchy — each directory can have its own layout that wraps all nested content.

**Page-to-layout data flow** — When `+page.ts` or `+page.server.ts` exports a `load` function that returns data, and `+page.svelte` receives it via `$page.data`, create `depends_on` edges from the page component to its load function module. Layout load functions feed data to all nested pages via `$page.data` merging.

**API route handlers** — When `+server.ts` exports named HTTP method functions (GET, POST, PUT, DELETE, PATCH), create edges from consuming components to the API handler based on fetch calls or form actions. Each exported method is a standalone endpoint.

**Form actions** — When `+page.server.ts` exports an `actions` object with named form handlers, create `depends_on` edges from the page component to its action handlers. Form actions are invoked via `use:enhance` or native form submission.

**$lib alias imports** — When a file imports from `$lib/...`, resolve to `src/lib/...`. Create `imports` edges from the consumer to the resolved module. `$lib` is the canonical way to share code across routes in SvelteKit.

**$app stores** — When a component uses `$page`, `$navigating`, or `$updated` from `$app/stores`, create `depends_on` edges to the SvelteKit runtime. These stores provide route-aware reactive state.

**$app/navigation functions** — When `goto()`, `invalidate()`, `pushState()`, or `replaceState()` are called from `$app/navigation`, create `depends_on` edges to the navigation module. These enable programmatic routing.

**$app/server utilities** — When `redirect()`, `error()`, `json()`, or `fail()` are called from `$app/server`, create `depends_on` edges. These are the standard way to handle responses in load functions and actions.

**Server/client boundary** — Files named `*.server.ts` or located in `src/lib/server/` run only on the server. `+page.server.ts` and `+layout.server.ts` are server-only. Create `depends_on` edges that cross this boundary and note the boundary in the edge description.

**Route groups** — Directories wrapped in parentheses `(group)` organize routes without affecting the URL path. Note these in node descriptions. They are used for shared layouts without URL impact.

**Adapter configuration** — When `svelte.config.js` configures an adapter (`@sveltejs/adapter-auto`, `@sveltejs/adapter-node`, `@sveltejs/adapter-static`, etc.), note the deployment target in the project description. The adapter determines how the app is built and deployed.

### Architectural Layers for SvelteKit

Assign nodes to these layers when detected:

| Layer ID | Layer Name | What Goes Here |
|---|---|---|
| `layer:ui` | UI Layer | `src/routes/` (+page.svelte, +layout.svelte, +error.svelte, +loading.svelte), `src/lib/components/` |
| `layer:service` | Service Layer | `src/lib/` (non-component), `src/routes/+page.ts`, `src/routes/+page.server.ts`, `src/routes/+layout.ts`, `src/routes/+layout.server.ts`, `src/lib/server/` |
| `layer:middleware` | Middleware Layer | `src/hooks.server.ts`, `src/hooks.client.ts` |
| `layer:utility` | Utility Layer | `src/params/`, pure utility functions in `src/lib/` |
| `layer:resource` | Resource Layer | `static/`, `src/app.html` |
| `layer:api` | API Layer | `src/routes/**/+server.ts` API route handlers |
| `layer:config` | Config Layer | `svelte.config.js`, `svelte.config.ts`, `vite.config.ts` |
| `layer:test` | Test Layer | `*.test.ts`, `*.spec.ts`, `tests/`, `__tests__/` |

### SvelteKit-Specific Patterns to Capture in languageLesson

- **File-based routing**: Routes are defined by the file-system structure under `src/routes/` — each `+page.svelte` maps to a URL, and directory structure determines the path hierarchy
- **Server load functions**: `+page.server.ts` and `+layout.server.ts` run only on the server, with direct access to databases and secrets — they return data that is serialized and sent to the client
- **Universal load functions**: `+page.ts` and `+layout.ts` run on both server and client — they can access `$app/stores` and are used for client-side navigation data fetching
- **Form actions**: The `actions` export in `+page.server.ts` handles form submissions with progressive enhancement — `use:enhance` enables SPA-like behavior while maintaining no-JS fallback
- **Layout composition**: Layouts nest via the file-system — each directory's `+layout.svelte` wraps all nested content, and `+layout.ts` data cascades down to children
- **$lib alias**: The `$lib` import alias resolves to `src/lib/` — it is the standard way to share code across routes, replacing relative `../../` imports
- **Adapters**: SvelteKit uses adapters to deploy to different platforms (`adapter-node`, `adapter-static`, `adapter-vercel`, `adapter-cloudflare`) — the adapter in `svelte.config.js` determines the build output format
- **Route groups**: Parenthesized directories like `(auth)` group routes for shared layouts without affecting the URL — useful for organizing routes with common UI or logic
- **Error boundaries**: `+error.svelte` components catch errors from load functions in their route segment — they receive `$page.error` and `$page.status` for rendering error UI
- **Hooks**: `src/hooks.server.ts` provides `handle` (intercepts every request) and `handleFetch` (intercepts fetch calls from server) — they are the SvelteKit equivalent of Express middleware
```

## 6. 注册变更

> **合并协调说明**：P1.5（Nuxt）和 P1.6（SvelteKit）均修改 `packages/core/src/languages/frameworks/index.ts`，新增各自的框架配置导入和注册。实施时需确保两个 spec 的变更合并到同一文件中，`builtinFrameworkConfigs` 数组应同时包含 `nuxtConfig` 和 `sveltekitConfig`。建议按以下顺序合并：
> 1. 先应用 P1.5 的变更（添加 `nuxtConfig`，排在 `vueConfig` 之前）
> 2. 再应用 P1.6 的变更（添加 `sveltekitConfig`，追加到数组末尾）
> 3. 最终数组顺序为：`reactConfig, nextjsConfig, nuxtConfig, expressConfig, vueConfig, sveltekitConfig`

### 6.1 文件: `packages/core/src/languages/frameworks/index.ts`

```typescript
import { sveltekitConfig } from "./sveltekit.js";

export const builtinFrameworkConfigs: FrameworkConfig[] = [
  reactConfig,
  nextjsConfig,
  expressConfig,
  vueConfig,
  sveltekitConfig,      // ← 新增
];

export {
  reactConfig,
  nextjsConfig,
  expressConfig,
  vueConfig,
  sveltekitConfig,      // ← 新增
};
```

### 6.2 无需修改的文件

| 文件 | 原因 |
|---|---|
| `packages/core/src/languages/configs/index.ts` | Svelte 语言配置已在 P0.2 中注册 |
| `packages/core/src/languages/configs/svelte.ts` | 无需修改，LanguageConfig 不变 |
| `packages/core/src/plugins/parsers/svelte-parser.ts` | 无需修改，SveltePlugin 不变 |
| `skills/understand/languages/svelte.md` | 无需修改，Svelte 语言指南不变 |
| `skills/understand/scan-project.mjs` | `.svelte` 映射已正确（P0.2） |
| `skills/understand/extract-import-map.mjs` | Svelte 导入解析已在 P0.2 中完成 |

## 7. 测试用例

### 7.1 框架检测测试（4 个）

| 测试用例 | 输入 | 预期输出 |
|---|---|---|
| 标准 SvelteKit 项目检测 | `package.json` 含 `"@sveltejs/kit": "^2.0.0"` | 检测为 `"sveltekit"` |
| Adapter 变体检测 | `package.json` 含 `"@sveltejs/adapter-node": "^4.0.0"` | 检测为 `"sveltekit"` |
| SvelteKit + adapter 同时存在 | `package.json` 含 `@sveltejs/kit` 和 `@sveltejs/adapter-static` | 检测为 `"sveltekit"`（不重复） |
| 非 SvelteKit 项目不误报 | `package.json` 含 `"svelte": "^5.0.0"` 但无 `@sveltejs/kit` | 不检测为 `"sveltekit"` |

### 7.2 层级映射测试（4 个）

| 测试用例 | 输入路径 | 预期层级 |
|---|---|---|
| 路由文件映射 | `src/routes/about/+page.svelte` | `ui` |
| 共享组件映射 | `src/lib/components/Button.svelte` | `ui`（优先于 `service`） |
| 共享逻辑映射 | `src/lib/utils/format.ts` | `service` |
| 服务端钩子映射 | `src/hooks.server.ts` | `middleware` |

### 7.3 回归测试

| 测试项 | 预期行为 |
|---|---|
| 现有 React 框架检测 | 行为不变 |
| 现有 Next.js 框架检测 | 行为不变 |
| 现有 Express 框架检测 | 行为不变 |
| 现有 Vue 框架检测 | 行为不变 |
| Svelte 语言配置 | 行为不变（P0.2） |
| SveltePlugin 解析 | 行为不变（P0.2） |
| `pnpm test` 全量通过 | 无失败用例 |
| `pnpm build` 成功 | 无编译错误 |

## 8. 验收标准

| 编号 | 验收条件 | 验证方式 |
|---|---|---|
| AC-1 | `sveltekitConfig` 通过 `FrameworkConfigSchema` 验证 | `FrameworkConfigSchema.safeParse(sveltekitConfig).success === true` |
| AC-2 | `sveltekitConfig.id` 为 `"sveltekit"` | 直接断言 |
| AC-3 | `sveltekitConfig.languages` 包含 `"svelte"` | `sveltekitConfig.languages.includes("svelte")` |
| AC-4 | 含 `@sveltejs/kit` 依赖的 `package.json` 可触发 SvelteKit 检测 | 框架检测单元测试 |
| AC-5 | 含 `@sveltejs/adapter-*` 依赖的 `package.json` 可触发 SvelteKit 检测 | 框架检测单元测试 |
| AC-6 | 仅含 `"svelte"` 依赖的 `package.json` 不触发 SvelteKit 检测 | 框架检测单元测试 |
| AC-7 | `layerHints` 映射覆盖 SvelteKit 核心目录 | 6 条映射全部存在 |
| AC-8 | `src/lib/components` 优先映射为 `ui`（而非 `service`） | 层级映射单元测试 |
| AC-9 | `sveltekit.md` Skill 文档存在且内容完整 | 文件存在 + 覆盖所有约定文件 |
| AC-10 | `builtinFrameworkConfigs` 包含 `sveltekitConfig` | `builtinFrameworkConfigs.find(c => c.id === "sveltekit")` 非空 |
| AC-11 | 所有现有测试通过 | `pnpm test` 无失败 |
| AC-12 | 构建成功 | `pnpm build` 无错误 |

## 9. 风险评估

| 风险 | 概率 | 影响 | 缓解措施 |
|---|---|---|---|
| SvelteKit 检测误报（纯 Svelte 项目被识别为 SvelteKit） | 低 | 错误注入 SvelteKit Skill 文档 | `detectionKeywords` 使用 `@sveltejs/kit` 和 `@sveltejs/adapter-` 前缀，纯 Svelte 项目不会包含这些依赖 |
| SvelteKit 检测漏报（SvelteKit 项目未被识别） | 低 | 无法注入框架特有提示词 | `@sveltejs/kit` 是所有 SvelteKit 项目的必须依赖，漏报概率极低 |
| layerHints 映射不覆盖自定义目录结构 | 中 | 部分文件无法自动分层 | layerHints 覆盖 SvelteKit 官方约定的目录结构；自定义目录由 LLM 根据上下文推断 |
| 与未来 SvelteKit 版本的兼容性 | 低 | 新增约定文件/目录未被识别 | Skill 文档覆盖 SvelteKit 2+ 的所有约定；新版本变化通过迭代更新 |
| `svelte.config.ts` 检测依赖文件存在性而非内容 | 低 | 非 SvelteKit 项目有此文件时产生干扰 | `manifestFiles` 仅作为辅助检测，主要依赖 `package.json` 中的 `detectionKeywords` |
